1.docker是什么？
2.什么是容器虚拟化技术？
3.docker怎么工作的？
4.容器是什么？
5.为什么docker比vm快？
6.常用命令
	6.1 查看docker版本
	6.2 查看docker完整信息
	6.3 查看docker帮助命令
	6.4 查看镜像的完整信息
	6.5 搜索centos、mysql、nginx、redis镜像，并下载centos、mysql、nginx、redis镜像为最新版本
	6.6 查看本地主机上的镜像
	6.7 查看本地主机上的所有镜像
	6.8 查看本地主机上的镜像（只显示镜像id）
	6.9 查看镜像的摘要信息
	6.10 删除centos镜像（根据镜像id）、同时删除mysql、nginx镜像（根据镜像名称：版本号）、删除全部
	6.11 下载centos镜像，新建并启动centos容器（名称为myCentos、交互式运行、主机端口为9999、容器端口为8888）
	6.12 容器停止退出，启动容器，重启容器，停止容器，启动容器，强制停止容器，开启容器
	6.13 列出当前所有正在运行的容器（当前正在运行的、当前正在运行的+历史、静默模式只显示容器编号）
	6.14 停止容器，删除centos容器，再次新建并启动centos容器，停止容器，一次性删除所有容器
	6.15 新建并启动centos容器（名称为myCentos、守护式运行、主机端口为9999、容器端口为8888）
	6.16 查看容器日志（最新、有时间戳、前15条）
	6.17 查看centos容器内的进程
	6.18 查看centos容器内的细节信息
	6.19 分别用两种方式重新进入centos容器，并说明两种方式的区别
	6.20 将日志文件从centos容器中拷贝到宿主机上的加家目录下
	6.21 查看日志并删除，停止并删除centos容器，删除所有镜像
7.什么是联合文件系统（UnionFS）？
8.镜像加载原理？
9.下载tomcat镜像并新建、启动tomcat容器（名称为myTomcat01、守护式运行、主机端口为8081、容器端口为8080）
10.进入myTomcat01容器中，删除doc目录，并提交为一个镜像（kun/myTomcat:1.1）
11.停止并删除myTomcat01容器，然后将tomcat镜像（主机端口为8081）和kun/	myTomcat:1.1镜像新建并启动容器，对比不一样，停止删除容器并删除所有镜像
12.docker容器数据卷是什么、能干吗？
13.

