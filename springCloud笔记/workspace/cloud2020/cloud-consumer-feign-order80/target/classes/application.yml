server:
  port: 80
eureka:
  client:
    register-with-eureka: false #不在注册中心注册自己。此order就是一个客户端，不作为微服务注册进注册中心。
    service-url:
      # defaultZone: http://eureka7001.com:7001/eureka, http://eureka7002.com:7002/eureka
      defaultZone: http://eureka7001.com:7001/eureka
ribbon:
  ReadTimeout: 5000
  ConnectTimeout: 5000
logging:
  level:
    com.kun.springcloud.service.PaymentFeignService: debug


